name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '*'

jobs:
  build_ruby:
    strategy:
      matrix:
        os: [ubuntu-22.04]
        ruby: ['3.4']
        test_task: [test]
        include:
          # JRuby/TruffleRuby run test:unit only (not the full test, which
          # also runs the asciidoc-testkit corpus): these legs exist to catch
          # Ruby-implementation compat bugs, not to re-validate fixture
          # content, which the CRuby legs already do exhaustively — and the
          # corpus's ~300 sequential `bundle exec` spawns (asciidoc-testkit
          # has no persistent-worker mode yet) multiply JVM startup cost into
          # a 10+ minute job.
          - os: ubuntu-22.04
            ruby: truffleruby-34.0.1
            test_task: test:unit
          - os: ubuntu-22.04
            ruby: jruby-9.4.14.0
            test_task: test:unit
          - os: macos-latest
            ruby: '3.4'
            test_task: test
          # Windows runs unit tests only (not the asciidoc-testkit corpus,
          # nor examples/release-notes conversion): the same ~300 sequential
          # `bundle exec` spawns that make the JRuby/TruffleRuby legs slow
          # are even slower on Windows's process-spawn overhead, and this
          # leg exists to catch Windows-specific path/line-ending bugs, not
          # to re-validate fixture content already covered exhaustively by
          # the Ubuntu/CRuby leg.
          - os: windows-latest
            ruby: '3.4'
            test_task: test:unit
    runs-on: ${{ matrix.os }}
    env:
      JRUBY_OPTS: '-J-XX:+TieredCompilation -J-XX:TieredStopAtLevel=1 -J-Xss2m -Xcompile.invokedynamic=false'
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Install Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
      - name: Install Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
      - name: Set JRUBY_OPTS
        if: matrix.jruby-opts
        run: echo 'JRUBY_OPTS=${{ matrix.jruby-opts }}' >> $GITHUB_ENV
      - name: Install dependencies
        run: bundle --jobs 3 --retry 3
      - name: Install npm dependencies
        run: npm ci
      - name: Run RuboCop
        run: bundle exec rubocop
      - name: Run tests
        run: bundle exec rake ${{ matrix.test_task }}
      - name: Run examples:convert
        if: matrix.os != 'windows-latest'
        run: bundle exec rake examples:convert
      - name: Run release_notes:convert
        if: matrix.os != 'windows-latest'
        run: bundle exec rake release_notes:convert
      # JS/Ruby parity, checked once (OS-independent Node.js code) on the
      # primary Linux/CRuby leg, which already has both toolchains installed.
      # continue-on-error because 2 cases (source-prettify, source-rouge) are
      # permanent, documented gaps — third-party Ruby-only syntax highlighters
      # never ported to the JS converter — not something a future commit can
      # regress-fix; a real regression would show as new failures beyond those.
      - name: Run testkit:test:js (JS/Ruby parity)
        if: matrix.os == 'ubuntu-22.04' && matrix.ruby == '3.4'
        continue-on-error: true
        run: bundle exec rake testkit:test:js
  build_js:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - name: Install Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
      # The JS parity tests compare the converter output byte-for-byte against
      # the Ruby reference converter, so the Ruby toolchain is required too.
      - name: Install Ruby
        uses: ruby/setup-ruby@v1
      - name: Install dependencies
        # The parity tests only need the asciidoctor runtime, not the
        # rubocop/testkit/etc. development group.
        run: |
          bundle config set --local without development
          bundle --jobs 3 --retry 3
          npm ci
      - name: Run tests
        run: npm test
      - name: Build single executable
        run: npm run build:sea
